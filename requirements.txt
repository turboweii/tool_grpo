# ========================================================================
# GRPO 依赖清单
# Week 3 Vanilla GRPO — veRL + vLLM V1 + tau-bench airline
# ========================================================================
#
# 安装顺序建议(避免依赖冲突):
#   1. pip install torch==2.7.0  (先装 PyTorch)
#   2. pip install -r requirements.txt
#   3. pip install flash-attn --no-build-isolation  (需 CUDA 编译)
#   4. pip install -e /workspace/verl  (veRL editable install)
#   5. pip install tau-bench/  (本地目录安装)
#
# 环境变量:
#   export VLLM_USE_V1=1
#   export CUDA_VISIBLE_DEVICES=0,1  (policy)

# ---- 核心训练框架 ----
# veRL 0.6.1 从本地 editable install，不要 pip install verl
# cd /workspace/verl && pip install -e .

# ---- PyTorch (CUDA 12.6) ----
torch==2.7.0
torchvision==0.22.0
torchaudio==2.7.0

# ---- vLLM (V1 Engine) ----
# 必须设置 VLLM_USE_V1=1
vllm==0.9.2

# ---- Transformers / PEFT ----
transformers>=4.51.0,<4.52.0
peft==0.18.1
trl>=0.11.0

# ---- 分布式 / 并行 ----
ray==2.55.1
accelerate>=0.34.0
deepspeed>=0.15.0

# ---- 注意力优化 ----
# flash-attn 需从源码编译安装:
#   pip install flash-attn --no-build-isolation
# 要求: CUDA 12.x, PyTorch 已安装
flash-attn>=2.4.0

# ---- 数据 ----
datasets>=3.0.0
pandas>=2.2.0
pyarrow>=17.0.0

# ---- 配置 ----
hydra-core>=1.3.2
omegaconf>=2.3.0

# ---- API 客户端 ----
openai>=1.40.0

# ---- 工具 ----
tqdm
pyyaml>=6.0
sentencepiece>=0.2.0
protobuf>=5.0.0

# ---- 量化 (optional) ----
bitsandbytes>=0.45.0

# ---- 日志 / 实验管理 ----
swanlab>=0.7.0
tensorboard>=2.20.0

# ---- 测试 ----
pytest>=8.0
pytest-asyncio>=0.24
